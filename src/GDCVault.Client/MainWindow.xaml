<ui:FluentWindow x:Class="GDCVault.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="GDC Vault" Height="560" Width="820"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <ui:Button Content="Adaugă" Icon="{ui:SymbolIcon Add24}"
                       DockPanel.Dock="Right" Click="OnAddClicked" />
            <TextBlock Text="GDC Vault" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
        </DockPanel>

        <Border Grid.Row="1" x:Name="ExpiringBanner" Background="#332E1A" CornerRadius="6"
                Padding="10" Margin="0,0,0,8" Visibility="Collapsed">
            <TextBlock x:Name="ExpiringBannerText" TextWrapping="Wrap" Foreground="#FFC060" />
        </Border>

        <ListView Grid.Row="2" x:Name="EntriesList" MouseDoubleClick="OnEntryDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nume" Width="260" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Tip" Width="180" DisplayMemberBinding="{Binding KindDisplay}" />
                    <GridViewColumn Header="Expiră" Width="140" DisplayMemberBinding="{Binding ExpiryDisplay}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</ui:FluentWindow>
